# ENSO Unified - Environment template
# Copy this to .env.local and fill in real values. Never commit .env or .env.local.

# Server-only secret. Used by /api/task/generate and /api/journal/generate.
# Get yours at https://console.anthropic.com/
ANTHROPIC_API_KEY=
